# التقرير — موظف AI مصري (مجمع الشفا الطبي)
تاريخ: 14-09-2026 | المجلد: D:\مشروعي

## 1) الفكرة
موظف استقبال ذكي يرد واتساب ويحجز مواعيد بكل اللهجات (مصري/خليجي/شامي/مغربي/فصحى) + فرز صحي (Triage) بدون أي تخمين تشخيصي.

## 2) اللي اتعمل بالترتيب
1. MVP حجوزات واتساب (Next-like Express + واتساب Cloud API موك)
2. داشبورد مريح للعين (فاتح/ليلي) + تجربة شات + حجوزات/رسائل
3. ذكاء متعدد التخصصات (12 تخصص) ثم تبسيط للحجوزات فقط بطلبك
4. إصلاح عبث الشات (خلط المواضيع + لوب "لا" + أزرار وهمية + undefined)
5. معالجة مشاكل الواقع:
   - منع الحجز المزدوج + جدول مواعيد + إجازات
   - "الساعة 5" الصبح/المغرب + التحقق من رقم 010/011/012/015
   - تحويل لبشري + تذكير 8 بليل
6. تحسينات العين + تقويم + باسورد + إلغاء/تعديل الحجز + فويس (Whisper + ميك المتصفح 🎤)
7. دعم كل اللهجات + تشكيل + أرقام مشرقية ٠١٢٣ + "بعد بكرة" + "بشحال/شقد"
8. ترقية كبيرة (طلبك "اعملهم كلهم"):
   - SQLite data.db بدل data.json + ترحيل تلقائي + جلسات محفوظة بعد الريستارت
   - تقويم لكل دكتور (days لكل دكتور وتعيين تلقائي للأقل حملاً)
   - تأكيد بعد التذكير (تأكيد/تمام) + إلغاء تلقائي للي ميأكدش قبل معاده بساعتين
   - حضور/غياب + تقارير (إيراد/حضور/غياب/نسبة/تخصصات) + ذاكرة المريض (ترحيب بالاسم)
   - حماية سبام 15/دقيقة + قوالب واتساب (WHATSAPP_REMINDER_TEMPLATE)
   - إصلاح خريطة الأيام (الأحد كان 6 بالغلط)
9. باسورد لوحة + نسخ احتياطي يومي 3 الفجر (آخر 7) + مراجعة حجوزات الليل 🌙 + متابعة الغايبين شهرياً
10. إصلاح 5 عيوب: تنبيه واتساب للاستقبال (STAFF_NOTIFY_PHONE)، إجازات استثنائية (closedDates/unavailableDates)، تضارب "تمام"، تحذير HTTPS للميك، نسخ خارجي (BACKUP_WEBHOOK_URL) + تحميل
11. خادم صحي ذكي (قديم — تمت إزالة التخمين لاحقاً في خطوة 20): كان يسمح "احتمال يكون عندك" + قاعدة health-knowledge.js + تخصيص الردود لكل اللهجات
12. ربط Groq (llama-3.3-70b-versatile) عبر OPENAI_BASE_URL — مفتاح مجاني اشتغل والشارة ظهرت ✅
13. إصلاحات تشغيل (شغلني.bat، EADDRINUSE، تشفير عربي)
14. تقييم الدكتور: يناسب مجمع/عيادة زحمة كاستقبال أول، بشرط إزالة التشخيص + ملف مريض + إلغاء برقم الحجز + صلاحيات + تشغيل رسمي
15. فحص شامل للردود والحجز وإصلاح 6 عيوب (طلبك "ازيك ليه بيرد كده" + "ظبط البرنامج"):
    - رد "ازيك" كان بيكمل حجز قديم من رقم test المشترك → كل متصفح بقى ليه رقم تجريبي لوحده (localStorage clinic_phone في public/index.html) + رسالة استكمال أوضح (ناقصني: اليوم/الرقم/الاسم + قول إلغاء) في src/ai.js
    - كشف عام / أنف وأذن / نفسية كان مستحيل يتحجز ("مفيش دكتور عام") → لو التخصص ملوش دكاترة أصلاً بيحجز على مستوى الخدمة (src/schedule.js:assignDoctor)
    - "احجز أسنان بكرة الساعة 5" + "المغرب" كان بينسى التخصص → حفظ التخصص قبل سؤال الصبح/المغرب (src/ai.js)
    - "الساعة 5" الغامضة كانت بتتسجل غلط لو فيه معاد قديم → دايماً بيسأل الأول
    - الاسم الجشع ("اسمي محمد عايز احجز" كان بيتسجل كله) → أول 2-3 كلمات بس
    - "لا عايز احجز" كانت بتتمسح كرفض + "تمام احجز" كانت بتتفهم تأكيد → الرفض والتأكيد بقوا للرسالة لوحدها فقط (src/ai.js + src/server.js)
16. نظام كامل (طلبك "اعمل كل الحاجات دي") — اتنفذ على نفس الـ stack:
    - Tools حقيقية src/tools.js (check_availability/book/cancel/reschedule/get_customer/transfer_to_human) + تأكيد إجباري قبل أي حجز: "هحجزلك ... هل تؤكد؟ (تأكيد/إلغاء)" في src/server.js
    - Roles: أدمن (DASHBOARD_PASSWORD) + استقبال (STAFF_PASSWORD) + حماية /api/config و /api/reset و /api/services للأدمن فقط + audit لكل حذف/تعديل
    - جداول جديدة في data.db: audit_log + notifications + kb_docs (src/store.js)
    - Knowledge Base: تبويب 📚 في اللوحة (إضافة/حذف) + البوت بيجاوب منها بدل الاختراع (نواة RAG بحث كلمات) + APIs: GET/POST/DELETE /api/kb
    - Analytics موسعة في GET /api/reports: دقة AI % + تحويل لبشري % + متوسط الرد + إجمالي المحادثات + أكثر الخدمات/الأسئلة + تبويب 📊 محدث
    - Dashboard: تبويبات جديدة 📚 المعرفة و 🛡️ سجل (Audit + Notifications) + إضافة خدمات/دكاترة من الإعدادات + APIs: /api/services /api/doctors /api/audit /api/notifications /api/role
    - AI Safety: منع Prompt Injection (تجاهل التعليمات/اكشف البرومبت) + منع تسريب بيانات العملاء + دعم إنجليزي أساسي (hello/price/book/cancel)
    - المتنفذش عمداً: مكالمات VoIP حقيقية (محتاجة Twilio وفلوس) + RAG بـ embeddings (محتاج Postgres/pgvector) + Email/SMS + rewrite لـ React/FastAPI
17. تحديث التقرير هذا
18. طلبات الدكتور المدفوعة (اتنفذت):
    - ملف مريض كامل: تاريخ مرضي/حساسية/أدوية/ملاحظات + سجل زيارات + إجمالي عربون (جدول patients بأعمدة جديدة + تبويب 👤 مرضى + APIs: GET /api/patients و GET/PATCH /api/patients/:phone)
    - عربون + تصدير إكسل: عمودا deposit/paid في bookings + زرار 💰 عربون في جدول الحجوزات + PATCH /api/bookings/:id/pay + تصدير GET /api/export.csv (يفتح في Excel) + إجمالي العربون في التقارير
    - متابعة لكل تخصص: recall_schedule (أسنان 180/جلدية 90/باطنة 30/قلب 30/أطفال 60/نسا 30/عظام 90/عيون 180/عام 90) + GET/POST /api/recall-schedule + المتابعة التلقائية أول كل شهر بقت حسب التخصص + POST /api/recall-now من غير days بيبعت حسب التخصص
    - رسالة جماعية: POST /api/broadcast {text,date} لكل حجوزات اليوم (مثال: الدكتور هيتأخر ساعة) + زرار 📢 في التقارير + تسجيل في audit و notifications
19. ID ثابت للمريض + بحث بالاسم (المريض غيّر رقمه):
    - عمود pid ثابت (1001...) في patients + ترحيل تلقائي للمرضى القدام + كل مريض جديد بياخد ID تلقائياً
    - بحث: GET /api/patients?q= بالاسم أو الرقم أو الـ ID + فتح الملف بـ GET /api/patients/:key (رقم أو ID)
    - تغيير الرقم: POST /api/patients/:phone/change-number {newPhone} — بينقل الملف والحجوزات والـ ID يفضل ثابت + زرار 🔄 تغيير الرقم في الملف + الـ ID ظاهر 🆔 في القايمة والملف
20. الحزمة الأمنية والطبية V1 (نقاط 5/6/7/14/15/18/21/42/44 من المراجعة):
    - شيل "احتمال يكون عندك X" نهائياً → فرز Triage فقط (معلومة عامة + علامات خطر + الدكتور يقيّم) في prompts/receptionist.md و health-knowledge.js و ai.js
    - Safety Layer قبل أي فهم: classifyUrgency من medical-knowledge.js — EMERGENCY (طوارئ فوراً + [طارئ]) / URGENT (تحويل لبشري برسالة مستعجلة وتنبيه 🔴) / NORMAL (يكمل حجز)
    - رقم حجز مقروء SCH-2026-000123 (عمود code + backfill لـ 17 حجز قديم) — يظهر في رسائل التأكيد واللوحة، والإلغاء/التعديل برقم الحجز مع تحقق الملكية (رقم غريب → رفض 🔒)
    - Idempotency: جدول processed_messages + فحص msgId في الـ webhook (رسالة واتساب مكررة لا تُعالج مرتين)
    - .gitignore جديد (.env و data.db و backups بره Git) + Health موسع (server/database/ai/whatsapp/backup/crons) + GET /api/cron-status + تبويب ⏰ في اللوحة
    - حجز ذري: createBookingChecked (فحص + إنشاء في BEGIN IMMEDIATE واحدة) + PRAGMA busy_timeout — وجُرب: حجز ثانٍ بنفس المعاد اترفض ببدائل
21. State Machine + Test Suite (أول 2 من أولويات V2):
    - src/booking-flow.js: IDLE → BOOKING (SERVICE→SLOT→CONTACT→CONFIRM→BOOKED) و CANCEL (CODE→CONFIRM→CANCELLED) و RESCHED (CODE→SLOT→CONFIRM→UPDATED) — تحقق مبكر من المعاد قبل طلب البيانات + تصحيح المعاد في خطوة التأكيد + Safety أولاً دائماً + توحيد الهمزات (أبغى/أريد) — مربوط في server.js قبل aiReply (أسعار/تحيات/صحة تكمل على القديم)
    - tests/run.js: 110 اختبار (npm test) — سلامة + مواعيد + مستخرجات + ردود + فلو + لهجات + اختراق + فرز + حواف + سلامة حجز + أدوار + OTP/زيارات/عام + عزل + إنتاج — بتنضف وراها تلقائياً
    - عيوب اتلقطت بالاختبارات واتصلحت: regex الكود كان أقصر من IDs القديمة + الهمزات (أبغى) مبتتمسكش + الرفض المسبق لغير المالك أصح من التأكيد الأول
22. أدوار + زيارات + OTP + تشديد (باقي أول 5 من V2):
    - src/roles.js: مصفوفة صلاحيات واحدة (ADMIN/DOCTOR/RECEPTION) + قائمة AI البيضاء (6 أدوات فقط — ممنوع: سجل طبي/تشخيص/حذف/تصدير/تغيير رقم) + إخفاء طبي ومالي حسب الدور
    - دخول حقيقي: جدول users (scrypt بملح + ترحيل تلقائي من sha256) + جلسات user_sessions (12 ساعة) + قفل 15 دقيقة بعد 5 فشل + إنشاء تلقائي من DASHBOARD/STAFF/DOCTOR_PASSWORD + تبويب 👥 (إضافة/إيقاف/حذف/ربط بدكتور) — والدخول القديم بالباسورد وحده شغال للتوافق
    - جدول visits (شكوى/ملاحظات/تشخيص/علاج — كتابة التشخيص للدكتور فقط) + APIs /api/visits + قسم 🩺 في ملف المريض (التشخيص مخفي عن الاستقبال)
    - OTP واتساب لتغيير الرقم: /api/otp/request (الكود على الرقم القديم + 10 دقائق + 5 محاولات + حد 5/ساعة) → /api/otp/confirm → النقل — والتغيير المباشر بقى أدمن فقط
    - تشديد: ترويسات أمان + فحص Origin + حد 1000 حرف للرسائل + تحقق النسخة POST /api/backup-verify (integrity للقراءة فقط) + مسح الرسائل القديمة POST /api/admin/purge (افتراضي 365 يوم) + بوابة الدكتور GET /api/doctor/today
    - التقارير: إيراد/حجوزات لكل دكتور + منحنى 14 يوم — والمالية مخفية عن غير الأدمن 🔒
23. موقع عام كامل + بوابة دكتور + دفع تجريبي (طلبك "موقع كامل من كل النواحي"):
    - public/site.html: رئيسية + خدمات وأسعار + دكاترة + حجز أونلاين (خدمة → يوم → مواعيد متاحة من التقويم → اسم/موبايل → تأكيد → كود) + تواصل واتساب — APIs عامة: /api/public/info و /api/public/slots (مواعيد فارغة بالدكتور) و POST /api/public/book (بنفس الذرية ومنع التضارب)
    - دعم ISO في parseSlotLabel ("2026-09-21 الساعة 11:00") + daySlots() جديدة في schedule.js
    - بوابة دكتور: ربط المستخدم بدكتور (عمود users.doctor) + GET /api/doctor/today (حجوزات الدكتور القادمة) + اللوحة تفلتر تلقائياً للدكتور
    - دفع Sandbox: جدول payments + POST /api/public/pay (نية بقيمة العربون) → confirm (تأكيد → مدفوع + إشعار) + زرار 💳 في صفحة الحجز — تُستبدل ببوابة حقيقية لاحقاً
    - جداول data.db الآن: + user_sessions + otp_codes + visits + payments
24. Multi-tenant حقيقي (طلبك "كذا دكتور/عيادة" → "اعمل الحل الصح"):
    - src/tenants.js: سجل العيادات + إعداد مستقل + اشتراك (تجربة 14 يوم ثم تفعيل/إيقاف) + ALS context + withTenant للـ cron والاختبارات
    - فصل كامل: tenants/{slug}/tenant.db + business-config.json + backups/ لكل عيادة — store.js اتحولت لـ Proxy يوجّه كل query لقاعدة العيادة الحالية (نفس الكود 100% من غير تكرار)
    - التوجيه: subdomain (shefa.domain.com) ← ?clinic= ← default — والموقع واللوحة والدخول بيحفظوا العيادة تلقائياً
    - واتساب لكل عيادة: توكن منفصل + فصل الرسائل برقم المستقبل phone_number_id + تحقق Meta لكل عيادة
    - سوبر أدمن: /super.html + /api/super/* (عيادات/تفعيل/إيقاف/حذف) بباسورد SUPERADMIN_PASSWORD المنفصل
    - الاشتراك المنتهي: الشات والحجز يردون برسالة دفع (402) واللوحة للقراءة + تنبيه 🔴 في اللوحة
    - الـ cron (تذكير/إلغاء/باك أب/متابعة) يعمل لكل عيادة على حدة + نسخ منفصلة
    - عيوب اتلقطت: ترحيل data.json كان يتنسخ للجدد (بقى للافتراضية فقط) + مسح عيادة والاتصال مفتوح يعمل crash (closeTenant قبل المسح)
    - tests: 104/104 ✅ (5 عزل: إنشاء/عزل/إعداد/اشتراك/مسح) + تنضيف قبل التشغيل (بقايا أي run واقع)
25. تجميل الموقع العام (طلبك — "أكتر حاجة هتفرق في عين الدكتور"):
    - إعادة تصميم site.html: نافبار ثابت + hero متدرج + إحصائيات + ليه إحنا + خدمات بأسعار + دكاترة بصور/أحرف + آراء مرضى بالنجوم + أسئلة شائعة أكورديون + حجز + تواصل + زرار واتساب عائم — متجاوب مع الموبايل
    - محتوى قابل للتحرير من اللوحة: tagline/about/testimonials/faqs في business-config.json + APIs (POST/DELETE /api/content للأدمن) + محررات وحذف في تبويب الإعدادات — وكل عيادة (Multi-tenant) محتواها مستقل
    - صور الدكاترة: حقل photo (رابط) مع fallback لأول حرف + hero_image جاهز
    - tests: 105/105 ✅
26. تجميل اللوحة (طلبك — نفس هوية الموقع، من غير مساس بالوظائف):
    - هيدر متدرج + شارات زجاجية + إحصائيات بشريط علوي + تبويبات بتتحرك + جداول بخطوط ملونة وhover + أزرار متدرجة + لينك الموقع العام في الهيدر + نفس الليلي المحسّن
    - صفحة الدخول بنفس الهوية — tests: 105/105 ✅
    - تحسين ثانٍ لليلي: خلفية متدرجة + تبويبات grid منتظمة + أيقونات إحصائيات + رسالة ترحيب داخل الشات الفاضي
27. دفعة الإنتاج (طلبك "اعملهم" — كل ما يتعمل بالكود):
    - تجربة استرجاع فعلية: POST /api/backup-restore-test (يفتح نسخة مؤقتة + integrity + عد الصفوف + مسح المؤقت) — وجُربت: ok مع 17 حجز
    - تشفير النسخ: AES-256-GCM بمفتاح BACKUP_KEY (64 hex) — النسخة المشفرة .enc والمكشوفة تتمسح + الفحص يفك التشفير + تحذير unencrypted في /api/health
    - Waitlist: جدول + عرض تلقائي عند امتلاء المعاد ("رد: انتظار") + تبليغ تلقائي لأول 3 عند أي إلغاء + تبويب في التقارير + APIs
    - مستندات المريض: جدول + رفع base64 (PDF/JPG/PNG حتى 5MB) + عرض للدكتور/الأدمن فقط + حذف للأدمن + قسم 📎 في الملف
    - Queue دائم: جدول jobs + عامل كل 5 ثواني لكل العيادات (retry 3 مرات) — التذكير والمتابعة والبث الجماعي بقوا عبر الطابور + /api/queue-status + سطر في اللوحة
    - RAG وسط: src/rag.js (تقسيم فقرات + TF-IDF + استشهاد [العنوان]) بدل بحث الكلمات + واجهة PgVectorProvider الجاهزة للمسار الكامل
    - نشر: Dockerfile + docker-compose.yml + nginx.sample.conf + .dockerignore
    - tests: 110/110 ✅
28. مراجعة أمنية وطبية نهائية على الكود (بدل ZIP خارجي — نفس checklist المراجع):
    - Safety قبل LLM فعلياً: aiReply يصنف أولاً — الطوارئ ترد مباشرة من غير ما تمر على Groq أصلاً (قرار محلي حتمي)
    - عزل الجلسات بين العيادات: مفاتيح الذاكرة المؤقتة = slug + رقم (الداتابيز كانت معزولة، الكاش لا — اتصلح + اختبار)
    - فحص بصمة الملفات (%PDF/JPEG/PNG من المحتوى مش المعلن) + الملفات BLOB خارج public + تحميل للدكتور فقط + منع path traversal (لا مسارات ملفات أصلاً)
    - فشل أي Cron → إشعار 🔴 في مركز الإشعارات (مش سجل فقط)
    - النسخة تشمل إعداد العيادة (config + tenant.json بنفس الطابع) — موت السيرفر الكامل قابل للاسترجاع
    - أنواع واتساب غير مدعومة (صورة/فيديو/لوكيشن) → رد واضح بدل دخولها الحجز
    - عيوب لقطتها المراجعة: "ألم صدر شديد" كانت URGENT (بقت EMERGENCY) + "دم" العارية كانت تلقط "الادمن" (اتشالت) + حالة انتحارية عدت NORMAL (بقت EMERGENCY فوراً)
    - تدقيق العزل: لا فتح مباشر لقواعد خارج Proxy إلا نسخ القراءة المؤقتة + الاختبارات (موثقة)
    - tests: 115/115 ✅
31. فصل التجربة عن الحقيقي (سؤالك "إيه لازمة المحادثة التجريبية"):
    - المحادثة التجريبية = محاكي للتدريب والاختبار من غير واتساب حقيقي — المريض الحقيقي بييجي واتساب/موقع فقط
    - المشكلة: التجارب كانت تتلوث في الداتا (إحصائيات وحجوزات وهمية) — الحل: POST /api/admin/purge-test (يمسح أرقام test/web-/01090x فقط) + زرار 🧪 في الإعدادات
    - tests: 119/119 ✅ (19 مجموعة)
29. مسح عام كامل على الكود (طلبك "بص بصه — زود أو صلح"):
    - عيوب حقيقية اتصلحت: الحجز في تاريخ فات كان ممكن عبر الـ API (بقى مرفوض + التقويم يخفيه) + تعديل الحجز لنفس المعاد كان يفشل بنفسه (استبعاد الذاتي) + الحذف النهائي كان متاحاً لأي دور (بقى أدمن فقط + تنبيه للاستقبال) + سب دومين محلي (a.localhost) مكنش يفصل العيادات + مواعيد التقويم العامة من غير حد سرعة
    - نضافة: شيلت كود ميت (void hacks + استيراد غير مستخدم) + تحقق من المدخلات (خدمات/محتوى/بث بحدود) + تنبيه 403 واضح في اللوحة
    - تدقيق: لا createBooking مباشر خارج المعاملة + كل الردود XSS-escaped + الجلسات fail-closed بين العيادات
    - tests: 117/117 ✅
30. نموذج مالك المنصة (طلبك "الدكتور مسؤول عن عيادته وأنا للأعطال فقط"):
    - طلب ذاتي: فورم "عايز النظام لعيادتك؟" في الموقع → POST /api/public/request-clinic (جدول clinic_requests في المنصة) → السوبر يوافق بضغطة (ينشئ العيادة + أدمن بباسورد لمرة واحدة) أو يرفض
    - مراقبة المنصة: GET /api/super/overview (اشتراك/حجوزات/أعطال cron/طابور/نسخ لكل العيادات) + قسمين جداد في /super.html (طلبات + نظرة)
    - دليل الدكتور: public/help.html (أول يوم/يومي/أمان/اشتراك) + لينك في اللوحة — عشان ميكلمكش في كل صغيرة
    - tests: 118/118 ✅
31. تحسينات اختيارية (قلت "يلا نعمله" — ببلاش الأول):
    - Pagination لكل القوائم الكبيرة: GET /api/bookings?page=&limit=&q= و patients/messages/audit/notifications (src/store.js:224 و src/server.js:652 حماية من 500+ حجز)
    - Email/SMS كقناة تانية: src/notify.js يبعت حقيقي لو SMTP_HOST/TWILIO متظبط وإلا mock + سجل — من غير مكتبة إجبارية
    - Thumbnails للصور: GET /api/docs/:id/thumb يجرب sharp وإلا يرجع الأصلي مع كاش + فحص البصمة السحرية باقي
    - Postgres للزحمة: docs/postgres-migration.md + واجهة PgVectorProvider في src/rag.js جاهزة — يُفعّل لما الحمل يستدعي
32. تفعيل Postgres محلياً + نشر ثابت (طلبك "لا انقلها عادي" + "ثابت مش كل شوية"):
    - ثبّت pg و pgvector (npm install) + شغّل Postgres 16 بـ Docker ( Mawazaf pgvector على 5433) — تم بنجاح + vector extension
    - سكريبت scripts/migrate-to-pg.mjs نقل 14 حجز و 3 مرضى من SQLite لـ Postgres (clinic_slug) — اختبار pg OK
    - ضاف PG_URL في .env (postgres://clinic:clinic123@localhost:5433/clinic) وخلى rag.js يقرا من Postgres لما موجود — SQLite يفضل للـ Pilot للسرعة
    - Fly.io: fly.toml (هيرو + mounts للـ backups/tenants) جاهز للنشر الثابت بدل ngrok الموقت
    - واتساب حقيقي: إنشاء تطبيق Shafa (ID 1875484520528146) + اختيار حالة "التواصل عبر واتساب" + تجهيز رقم اختباري 1-555-166-7539 و Phone ID 1296879176849755 + حفظهم في .env — التعثر الحالي: Verify بيلف بسبب صفحة تحذير ngrok المجاني (الحل: النشر على Fly/Domain ثابت)

## 3) التقنية
- Node 24 + Express + node:sqlite (DatabaseSync) + node-cron + axios + openai SDK
- ملفات: src/server.js, ai.js, tools.js, booking-flow.js, roles.js, auth.js, otp.js, tenants.js, queue.js, rag.js, notify.js, store.js, schedule.js, medical-knowledge.js, health-knowledge.js, voice.js, whatsapp.js, business-config.json, prompts/receptionist.md, public/index.html, public/login.html, public/site.html, public/super.html, public/help.html, tests/run.js, fly.toml, scripts/migrate-to-pg.mjs, docs/postgres-migration.md, data.db, tenants/, backups/, Dockerfile, docker-compose.yml, nginx.sample.conf, شغلني.bat, .env
- جداول data.db: bookings, messages, handoffs, patients, sessions, sender_phones, recall_log, audit_log, notifications, kb_docs, processed_messages, cron_runs, users, user_sessions, otp_codes, visits, payments, waitlist, documents, jobs, clinic_requests (+ Postgres: نفس الجداول + clinic_slug + embedding vector(1536) لـ kb_docs)
- patients: phone/name/visits/lastService/lastAt + history/allergies/meds/notes/birth/gender + pid (ID ثابت)
- bookings: + code (SCH-2026-...) + deposit/paid + needs_review
- المنافذ: 3000 (قابل للتغيير PORT) | cron: تذكير 20:00، إلغاء */30، باك أب 03:00، متابعة 10:00 يوم 1

## 4) التشغيل
- شغل: دبل كليك "شغلني" (يقفل القديم ويفتح http://localhost:3000)
- .env المطلوب:
  PORT=3000
  DASHBOARD_PASSWORD=xxx123 (أدمن — كل الصلاحيات)
  STAFF_PASSWORD=yyy456 (استقبال — حجوزات ومسجات بس، اختياري)
  DOCTOR_PASSWORD=zzz789 (دكتور — طبي، اختياري)
  SUPERADMIN_PASSWORD=sss000 (مالك المنصة — /super.html)
  BACKUP_KEY=(64 hex لتشفير النسخ — ولّده بالأمر في .env.example)
  OPENAI_API_KEY=gsk_...
  OPENAI_BASE_URL=https://api.groq.com/openai/v1
  OPENAI_MODEL=llama-3.3-70b-versatile
  WHATSAPP_TOKEN / PHONE_NUMBER_ID / VERIFY_TOKEN (للإنتاج)
  STAFF_NOTIFY_PHONE=01... (تنبيه البشري)
  BACKUP_WEBHOOK_URL=... (اختياري)
  RECALL_DAYS=180
  WHATSAPP_REMINDER_TEMPLATE / LANG
- الإجازات: schedule.closedDates / doctors[].unavailableDates / vacationUntil في business-config.json
- أزرار: 🎤 ميك (Chrome + HTTPS على السيرفر)، 🆕 محادثة جديدة، تأكيد/حضر/غايب/إلغاء، نسخ/تحميل

## 5) الضوابط
- فرز صحي فقط (Triage): معلومة عامة + علامات خطر + "الأعراض لها أسباب متعددة والدكتور يقيّم بالكشف" — ممنوع تخمين مرض أو "احتمال يكون عندك"
- 3 مستويات: EMERGENCY (طوارئ فوراً) / URGENT (بشري مستعجل 🔴) / NORMAL (حجز عادي) — الـ Safety قبل أي رد
- ممنوع: تشخيص قاطع، جرعات، تفسير تحاليل كتشخيص
- طوارئ → [طارئ] + رقم الطوارئ
- AI Safety جديد: تأكيد إجباري قبل الحجز (تأكيد/إلغاء) + منع Prompt Injection + منع تسريب بيانات العملاء + رفض "هات أرقام الناس"
- أي حجز/إلغاء/تعديل/دخول بيتسجل في audit_log + الإشعارات في notifications

## 6) المتبقي للإنتاج الحقيقي
- سيرفر VPS + دومين HTTPS + رقم واتساب رسمي
- مفتاح OpenAI للفويس الصوتي على واتساب (Groq لا يفرغ صوت)
- RAG حقيقي بـ embeddings (Postgres/pgvector) بدل RAG الوسط + Email/SMS + مكالمات VoIP (Twilio)

## 6ب) خطة الإطلاق خطوة بخطوة (اللي محتاج فلوس/حسابات — بالترتيب والتكلفة التقريبية)
1. VPS (250-500ج/شهر: Hostinger/Contabo 4GB) + دومين (~300ج/سنة) + سجل A للدومين على IP السيرفر.
2. على السيرفر: `docker compose up -d --build` (يقرا .env من الهوست — متنساش BACKUP_KEY المولد بـ `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`).
3. Nginx من nginx.sample.conf + `certbot --nginx -d clinic-domain.com` (HTTPS إجباري للواتساب).
4. Meta developers.facebook.com → WhatsApp API Setup → رقم رسمي → حط WHATSAPP_TOKEN و PHONE_NUMBER_ID (أو لكل عيادة من /super.html) → Callback: `https://clinic-domain.com/webhook?clinic=slug` بنفس VERIFY_TOKEN.
5. قوالب التذكير: أنشئ reminder_1 في مدير واتساب واعتمدها → WHATSAPP_REMINDER_TEMPLATE (إلزامية بعد 24 ساعة من آخر رسالة).
6. الدفع الحقيقي (مصر: Paymob/Fawry): افتح حساب تاجر → استبدل POST /api/public/pay/confirm بالـ webhook بتاعهم (التوقيع يتحقق قبل paid=1) — الكود الحالي Sandbox بواجهة مطابقة.
7. أول تشغيل: POST /api/backup-restore-test لازم ok + جرّب استرجاع يدوي كامل مرة (وقف السيرفر → انسخ أحدث نسخة مكان data.db → شغل → اتأكد).
8. RAG الكامل لاحقاً: فعّل خدمة db في compose + PG_URL + مفتاح embeddings + نفّذ PgVectorProvider (الواجهة في src/rag.js).

## 7) ملخص الشات
- طلبت أفكار مشاريع → رشحت 3 → اخترت موظف AI مصري حجوزات
- بنينا MVP → حسنّا الشكل → حسّنا الذكاء لكل التخصصات → بسطناه للحجوزات فقط
- صلحنا عبث "لا" والخلط والأزرار الوهمية → سألت عن مشاكل الواقع → نفذنا منع التضارب/الغموض/الرقم/التحويل/التذكير
- طلبت كل اللهجات → نفذنا → طلبت الفويس وتعديل المعاد → نفذنا → طلبت الباسورد والمتابعة والباك أب والمراجعة → نفذنا
- ربطت Groq → فعلنا الباسورد خطوة بخطوة → حولنا الخادم لفرز صحي (Triage فقط بعد إزالة التخمين في خطوة 20)
- سألت "لو انت الدكتور هتقول ايه" → قيمناه كدكتور (80% جاهز، ناقصه ملف مريض وصلاحيات وتشغيل رسمي)
- سألت "ليه بيرد كده على ازيك" → شرحنا pending + صلحنا 6 عيوب حجز وردود
- بعت مواصفات AI Receptionist الكاملة (15 قسم) → نفذناها على نفس الـ stack (Tools + تأكيد + Roles + Audit + KB + Analytics + Safety + إنجليزي)
- سألت "STAFF_PASSWORD احطه فين" → اتضاف في .env (أدمن/استقبال)
- طلبت طلبات الدكتور المدفوعة → نفذنا ملف مريض كامل + عربون وإكسل + متابعة لكل تخصص + رسالة جماعية
- سألت "المريض غيّر رقمه" → ضفنا ID ثابت + بحث بالاسم/ID + نقل الملف لرقم جديد
- طلبت أولويات V2 (اخترت لك: State Machine + Tests) → نفذنا booking-flow.js + tests/run.js (99/99 ✅)
- قلت "اعملهم" (أدوار + زيارات + OTP + تشديد) → نفذنا roles/auth/visits/OTP/hardening
- سألت "هل الموقع ليه api" → وثقنا كل الـ APIs
- طلبت "موقع كامل من كل النواحي" (عام + إدارة) → بنينا site.html + بوابة دكتور + دفع Sandbox + تقارير موسعة
- طلبت Multi-tenant لكذا عيادة → نفذنا tenants.js + عزل كامل + سوبر أدمن + اشتراكات (tests 104/104 ✅)
- طلبت تجميل الموقع (أهم حاجة في عين الدكتور) → صممنا site.html + محتوى قابل للتحرير (tests 105/105 ✅)
- طلبت تجميل اللوحة بنفس الهوية → هيدر متدرج + دخول محسنة (105/105 ✅)
- سألت إيه اللي ناقص → checklist الإطلاق (تشغيل مش برمجة)
- طلبت دفعة الإنتاج (استرجاع/تشفير/انتظار/مستندات/طابور/RAG/Docker) → اتنفذت (tests 110/110 ✅)
- هذا التقرير بطلبك (محدث بكل ما سبق — آخر مراجعة شاملة)

## 8) أوامر سريعة
- الموقع العام: /site.html | معلومات: GET /api/public/info | مواعيد: GET /api/public/slots?service=&date= | حجز: POST /api/public/book | دفع: POST /api/public/pay → /api/public/pay/confirm
- اختبار صحة: GET /api/health (server/database/ai/whatsapp/backup/crons) | مهام: GET /api/cron-status
- تجربة: POST /api/chat {phone,text} (محتاج كوكي دخول)
- دخول: POST /api/login {username,password} (أو {password} قديم) → {role: admin|doctor|reception} | دوري: GET /api/role | خروج: POST /api/logout
- مستخدمون (أدمن): GET/POST /api/users | PATCH/DELETE /api/users/:username
- حضر: PATCH /api/bookings/:id {status: "attended"|"no_show"}
- تذكير فوري: POST /api/remind-now
- إلغاء تلقائي: POST /api/autocancel-now
- متابعة: POST /api/recall-now {days}
- باك أب: POST /api/backup-now | GET /api/backup-download | فحص: POST /api/backup-verify (أدمن)
- مسح قديم: POST /api/admin/purge {days} (أدمن)
- إحصاء: GET /api/stats | /api/reports | /api/handoffs | حجوزاتي: GET /api/doctor/today
- معرفة: GET /api/kb | POST /api/kb {title,body} (أدمن) | DELETE /api/kb/:id (أدمن)
- خدمات: POST /api/services {name,price,specialty} (أدمن) | دكاترة: POST /api/doctors {name,specialty} (أدمن)
- سجل: GET /api/audit (أدمن) | إشعارات: GET /api/notifications
- مرضى: GET /api/patients?q= (اسم/رقم/ID) | GET/PATCH /api/patients/:key (الطبي للدكتور) | نقل مباشر: POST /api/patients/:phone/change-number (أدمن) | OTP: POST /api/otp/request → /api/otp/confirm
- زيارات (دكتور): POST/PATCH /api/visits | DELETE (أدمن)
- عربون: PATCH /api/bookings/:id/pay {deposit,paid} | إكسل: GET /api/export.csv (أدمن)
- بث جماعي: POST /api/broadcast {text,date} | متابعة تخصصات: GET/POST /api/recall-schedule
- اختبارات: npm test (119 حالة: +1 مسح التجربة)
- طلبات: POST /api/public/request-clinic | إدارة: GET /api/super/requests + POST موافقة/رفض | نظرة: GET /api/super/overview | مساعدة: /help.html
- نسخ: POST /api/backup-verify | استرجاع تجريبي: POST /api/backup-restore-test (أدمن)
- انتظار: GET /api/waitlist | تبليغ: POST /api/waitlist/:id/notify
- مستندات: POST /api/docs | GET /api/docs?patient= | GET /api/docs/:id (دكتور) | DELETE (أدمن)
- طابور: GET /api/queue-status (أدمن)
- سوبر أدمن: /super.html | عيادات: GET/POST /api/super/tenants | تفعيل/إيقاف: PATCH | حذف: DELETE (باسورد SUPERADMIN_PASSWORD)
- عيادة معينة محلياً: ?clinic=slug (يتحفظ) أو subdomain في الإنتاج
